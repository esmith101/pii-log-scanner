{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); if echo \"$INPUT\" | grep -q 'git push'; then printf '{\"systemMessage\":\"⚠️  About to push — have you scanned for PII in log statements? Run: /scan-pii to check before this push.\"}'; fi; exit 0"
          }
        ]
      }
    ]
  }
}
